<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>動画のシリーズ一覧</title>
        <link rel="stylesheet" href="/css/style.css" />
        <script src="/js/jquery.min.js"></script>
        <script>
            $(() => {
                $("#marks").change(() => {
                    let mark = $("#marks").val();
                    if (mark != "0") {
                        location.href = "/videos/series?mark=" + mark;
                    }
                    else {
                        location.href = "/videos/series?mark=0";
                    }
                });

            })
        </script>
    </head>
    <body>
        <header>
            <h1>動画のシリーズ一覧</h1>
            <p class="comment">Ctrl+F で検索できます。</p>
            <p class="comment">現在のマークによる絞り込み：<%= mark %></p>
            <div class="menu">
                マークで絞り込む <select id="marks">
                    <option value="0">-----</option>
                    <% for (let m of marks) { %>
                        <option value="<%= m %>"><%= m %></option>
                    <% } %>
                </select>
                &nbsp;|&nbsp;<a href="javascript:location.href='/videos/series'">絞り込みしない。</a>
            </div>
        </header>
        <article>
            <p class="message"><%= message %></p>
            <ol>
                <% for (let s of result) { %>
                    <li><a href="/videos/showContent?series=<%= s %>" target="_blank"><%= s %></a></li>
                <% } %>
            </ol>
        </article>
        <footer>
            <p>&nbsp;</p>
            <p class="footer_text"><a href="#top">TOP</a></p>
            <p>&nbsp;</p>
            <p>&nbsp;</p>
        </footer>
    </body>
</html>