<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>プレイリスト：<%= title %></title>
        <link rel="stylesheet" href="/css/style.css" />
        <style>
            audio {
                padding:10px;
                margin-left:20%;
            }
            ol {
                font-size: 11pt;
                color:blue;
            }
        </style>
    </head>

    <body>
        <header>
            <h1>プレイリスト：<%= title %></h1>
            <p class="comment">うまく演奏できない場合は、すべてのパスが正しいか確認してください。</p>
        </header>
        <article>
            <br />
            <audio id="music" controls>
                <% for (let item of items) { %>
                    <% if (item) { %>
                        <source src="/music/getmusic?path=<%= item %>" />
                    <% } %>
                <% } %>
            </audio>
            <br />
            <h3>曲目</h3>
            <ol>
                <% for (let item of items) { %>
                    <% if (item) { %>
                        <li><%= item %></li>
                    <% } %>
                <% } %>
            </ol>
        </article>
        <footer>
            <p>&nbsp;</p>
            <p>&nbsp;</p>
        </footer>
    </body>
</html>